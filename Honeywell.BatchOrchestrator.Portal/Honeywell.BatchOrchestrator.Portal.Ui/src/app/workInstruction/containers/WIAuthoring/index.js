import WIAuthoring from './WIAuthoring';
import { withWorkInstructionsContext } from '../../../../controllers/work-instructions/work-instructions-context';

export default withWorkInstructionsContext(WIAuthoring);
