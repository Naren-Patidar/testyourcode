import AddTaskListModal from './AddTaskListModal';
import { withWorkInstructionsContext } from '../../../../controllers/work-instructions/work-instructions-context';

export default withWorkInstructionsContext(AddTaskListModal);
